import * as THREE from "three";
class HoverEventHandler {
}
HoverEventHandler.handle = (event, camera, scene, renderer, onPointerOverObject, onPointerOverEmpty) => {
  const rect = renderer.domElement.getBoundingClientRect();
  const mouse = new THREE.Vector2();
  mouse.x = (event.clientX - rect.left) / rect.width * 2 - 1;
  mouse.y = -((event.clientY - rect.top) / rect.height) * 2 + 1;
  const raycaster = new THREE.Raycaster();
  raycaster.setFromCamera(mouse, camera);
  const intersects = raycaster.intersectObjects(scene.children, true);
  if (intersects.length === 0) {
    onPointerOverEmpty();
    return;
  }
  const intersection = intersects[0];
  if (intersection.object.type === "Mesh" && "isPartOfThreeJs" in intersection.object.userData && intersection.object.userData["isPartOfThreeJs"] === true && intersection.object.parent?.type === "Group" && "isPartOfThreeJs" in intersection.object.parent.userData && intersection.object.parent.userData["isPartOfThreeJs"] === true && "instancedMeshId" in intersection.object.userData && "modelId" in intersection.object.parent.userData && intersection.instanceId != void 0) {
    const instancedMeshId = intersection.object.userData["instancedMeshId"];
    const modelId = intersection.object.parent.userData["modelId"];
    onPointerOverObject({ modelId, instancedMeshId, instancedMeshInstanceIndex: intersection.instanceId });
  } else onPointerOverEmpty();
};
export {
  HoverEventHandler
};
//# sourceMappingURL=hover.mjs.map