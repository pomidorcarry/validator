import * as THREE from 'three';
import { MeshInstanceId, ExpressId } from '../types.mjs';

/**
 * MeshInstance represents a single instanced mesh item.
 */
declare class MeshInstance {
    id: MeshInstanceId;
    expressId: ExpressId;
    transformationMatrix: THREE.Matrix4;
    color: THREE.Color;
    isVisible: boolean;
    isHighlighted: boolean;
    isSelected: boolean;
    constructor(expressId: ExpressId, transformationMatrix: THREE.Matrix4, color: THREE.Color);
}

export { MeshInstance };
