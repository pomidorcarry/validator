import * as flatbuffers from "flatbuffers";
class SpatialStructureNode {
  constructor() {
    this.bb = null;
    this.bb_pos = 0;
  }
  __init(i, bb) {
    this.bb_pos = i;
    this.bb = bb;
    return this;
  }
  static getRootAsSpatialStructureNode(bb, obj) {
    return (obj || new SpatialStructureNode()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
  }
  static getSizePrefixedRootAsSpatialStructureNode(bb, obj) {
    bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
    return (obj || new SpatialStructureNode()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
  }
  expressId() {
    const offset = this.bb.__offset(this.bb_pos, 4);
    return offset ? this.bb.readUint64(this.bb_pos + offset) : BigInt("0");
  }
  children(index, obj) {
    const offset = this.bb.__offset(this.bb_pos, 6);
    return offset ? (obj || new SpatialStructureNode()).__init(this.bb.__indirect(this.bb.__vector(this.bb_pos + offset) + index * 4), this.bb) : null;
  }
  childrenLength() {
    const offset = this.bb.__offset(this.bb_pos, 6);
    return offset ? this.bb.__vector_len(this.bb_pos + offset) : 0;
  }
  static startSpatialStructureNode(builder) {
    builder.startObject(2);
  }
  static addExpressId(builder, expressId) {
    builder.addFieldInt64(0, expressId, BigInt("0"));
  }
  static addChildren(builder, childrenOffset) {
    builder.addFieldOffset(1, childrenOffset, 0);
  }
  static createChildrenVector(builder, data) {
    builder.startVector(4, data.length, 4);
    for (let i = data.length - 1; i >= 0; i--) {
      builder.addOffset(data[i]);
    }
    return builder.endVector();
  }
  static startChildrenVector(builder, numElems) {
    builder.startVector(4, numElems, 4);
  }
  static endSpatialStructureNode(builder) {
    const offset = builder.endObject();
    builder.requiredField(offset, 6);
    return offset;
  }
  static createSpatialStructureNode(builder, expressId, childrenOffset) {
    SpatialStructureNode.startSpatialStructureNode(builder);
    SpatialStructureNode.addExpressId(builder, expressId);
    SpatialStructureNode.addChildren(builder, childrenOffset);
    return SpatialStructureNode.endSpatialStructureNode(builder);
  }
}
export {
  SpatialStructureNode
};
//# sourceMappingURL=spatial-structure-node.mjs.map