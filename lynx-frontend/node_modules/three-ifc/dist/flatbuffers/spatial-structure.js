"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var spatial_structure_exports = {};
__export(spatial_structure_exports, {
  SpatialStructure: () => SpatialStructure
});
module.exports = __toCommonJS(spatial_structure_exports);
var flatbuffers = __toESM(require("flatbuffers"));
var import_spatial_structure_node = require("./spatial-structure-node");
class SpatialStructure {
  constructor() {
    this.bb = null;
    this.bb_pos = 0;
  }
  __init(i, bb) {
    this.bb_pos = i;
    this.bb = bb;
    return this;
  }
  static getRootAsSpatialStructure(bb, obj) {
    return (obj || new SpatialStructure()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
  }
  static getSizePrefixedRootAsSpatialStructure(bb, obj) {
    bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
    return (obj || new SpatialStructure()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
  }
  node(obj) {
    const offset = this.bb.__offset(this.bb_pos, 4);
    return offset ? (obj || new import_spatial_structure_node.SpatialStructureNode()).__init(this.bb.__indirect(this.bb_pos + offset), this.bb) : null;
  }
  static startSpatialStructure(builder) {
    builder.startObject(1);
  }
  static addNode(builder, nodeOffset) {
    builder.addFieldOffset(0, nodeOffset, 0);
  }
  static endSpatialStructure(builder) {
    const offset = builder.endObject();
    builder.requiredField(offset, 4);
    return offset;
  }
  static createSpatialStructure(builder, nodeOffset) {
    SpatialStructure.startSpatialStructure(builder);
    SpatialStructure.addNode(builder, nodeOffset);
    return SpatialStructure.endSpatialStructure(builder);
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  SpatialStructure
});
//# sourceMappingURL=spatial-structure.js.map