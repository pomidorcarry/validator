import * as WebIfc from 'web-ifc';
import { Geometry } from '../../graphics/geometry.js';
import 'three';

declare class WebIfcGeometryConverter {
    static convert(webIfcApi: WebIfc.IfcAPI, webIfcGeometry: WebIfc.IfcGeometry): Geometry;
}

export { WebIfcGeometryConverter };
