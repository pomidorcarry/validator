import { Geometry } from '../../graphics/geometry.js';
import 'three';

declare class FlatbuffersGeometryConverter {
    static toFlatbuffersBinary(geometry: Geometry): Uint8Array;
    static fromFlatbuffersBinary(buffer: Uint8Array<ArrayBufferLike>): Geometry;
}

export { FlatbuffersGeometryConverter };
