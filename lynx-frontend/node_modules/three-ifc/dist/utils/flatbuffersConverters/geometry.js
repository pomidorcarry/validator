"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var geometry_exports = {};
__export(geometry_exports, {
  FlatbuffersGeometryConverter: () => FlatbuffersGeometryConverter
});
module.exports = __toCommonJS(geometry_exports);
var flatbuffers = __toESM(require("flatbuffers"));
var import_schema = require("../../flatbuffers/schema");
var import_graphics = require("../../graphics");
class FlatbuffersGeometryConverter {
  static toFlatbuffersBinary(geometry) {
    const builder = new flatbuffers.Builder();
    const vertexOffset = import_schema.Geometry.createVerticesVector(builder, geometry.vertices);
    const normalOffset = import_schema.Geometry.createNormalsVector(builder, geometry.normals);
    const indexOffset = import_schema.Geometry.createIndicesVector(builder, geometry.indices);
    const geometryOffset = import_schema.Geometry.createGeometry(builder, vertexOffset, normalOffset, indexOffset);
    builder.finish(geometryOffset);
    return builder.asUint8Array();
  }
  static fromFlatbuffersBinary(buffer) {
    const byteBuffer = new flatbuffers.ByteBuffer(buffer);
    const flatbuffersGeometry = import_schema.Geometry.getRootAsGeometry(byteBuffer);
    const vertices = flatbuffersGeometry.verticesArray();
    const normals = flatbuffersGeometry.normalsArray();
    const indices = flatbuffersGeometry.indicesArray();
    if (vertices == null || normals == null || indices == null) throw new Error("Failed to convert flatbuffers binary to geometry");
    return new import_graphics.Geometry(vertices, normals, indices);
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  FlatbuffersGeometryConverter
});
//# sourceMappingURL=geometry.js.map